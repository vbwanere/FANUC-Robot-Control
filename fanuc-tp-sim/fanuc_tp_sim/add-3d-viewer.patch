--- a/fanuc_tp_sim/__main__.py
+++ b/fanuc_tp_sim/__main__.py
@@ -102,6 +102,11 @@
     return 0
 
 
+def cmd_view(args):
+    from .viewer import main as vmain
+    return vmain(args.path, args.prog, args.follow)
+
+
 def main(argv=None):
     ap = argparse.ArgumentParser(prog="fanuc_tp_sim")
     sub = ap.add_subparsers(dest="cmd", required=True)
@@ -123,6 +128,13 @@
     p.add_argument("prog", nargs="?")
     p.set_defaults(fn=cmd_pendant)
 
+    p = sub.add_parser("view", help="3D view (needs matplotlib)")
+    p.add_argument("path")
+    p.add_argument("prog", nargs="?")
+    p.add_argument("--follow", action="store_true",
+                   help="mirror a running pendant instead of simulating")
+    p.set_defaults(fn=cmd_view)
+
     a = ap.parse_args(argv)
     return a.fn(a)
 
--- a/fanuc_tp_sim/pendant.py
+++ b/fanuc_tp_sim/pendant.py
@@ -6,12 +6,14 @@
 I/O screens, and TOUCHUP that writes the current pose back into P[n].
 """
 import curses
+import socket
 import time
 
 from . import kin
 from .lsparse import Program
 from .runtime import Controller
 from .cell import Cell, DOOR_Y, INFEED, CHUCK, OUTFEED
+from .viewer import broadcast
 
 SCREENS = ["EDIT", "DATA R", "DATA PR", "I/O", "FRAMES", "POSN", "HELP"]
 COORDS = ["JOINT", "WORLD", "TOOL", "USER"]
@@ -63,6 +65,7 @@
         self.iokind = "DI"
         self.framecur = 1
         self.frametype = "UTOOL"
+        self.sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
 
     # ------------------------------------------------------------------ loop
     def run(self):
@@ -74,6 +77,7 @@
             dt = min(0.1, now - last)
             last = now
             self.tick(dt)
+            broadcast(self.sock, self.c, self.cell)
             self.draw()
             ch = self.s.getch()
             if ch != -1:
